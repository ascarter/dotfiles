# Dotfiles are applied only by explicit `mise dotfiles apply` or
# `mise bootstrap` operations.
min_version = "2026.7.4"

[settings]
dotfiles.default_mode = "symlink"

# Keep the XDG config directory open to unmanaged and application-owned files.
# Each source file is linked individually rather than replacing ~/.config.
[dotfiles]
"~/.config" = { source = "../src/config", mode = "symlink-each" }

[dotfiles."~/.zshenv/xdg"]
comment = "#"
block = '''
export XDG_BIN_HOME="${XDG_BIN_HOME:-$HOME/.local/bin}"
export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
export ZDOTDIR="${ZDOTDIR:-$XDG_CONFIG_HOME/zsh}"

# Rustup owns Rust toolchains even when mise selects them. Keep its shared
# toolchains and Cargo state beneath the XDG data directory.
export RUSTUP_HOME="${RUSTUP_HOME:-$XDG_DATA_HOME/rustup}"
export CARGO_HOME="${CARGO_HOME:-$XDG_DATA_HOME/cargo}"

typeset -U path PATH
path=("$XDG_BIN_HOME" $path)
'''

[bootstrap.user]
login_shell = "/bin/zsh"

[tasks.bootstrap]
description = "Reconcile the user environment"
run = [{ task = "bootstrap-git" }]
