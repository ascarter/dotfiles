-- Plugins are auto-loaded from this directory by lazy.nvim.
-- Each file should return a table (or list of tables) of plugin specs.
--
-- Example:
--   return {
--     "github/user-repo",
--     event = "VeryLazy",
--     config = function()
--       require("plugin").setup({})
--     end,
--   }

return {}
